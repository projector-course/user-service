const STAGE = {
  DEVELOPMENT: 'development',
  PRODUCTION: 'production',
};

const LOGGER_LEVEL = {
  ERROR: 'error',
  WARN: 'warn',
  INFO: 'info',
  DEBUG: 'debug',
};

const LOGGER_TARGET = {
  CONSOLE: 'console',
  STDOUT: 'stdout',
};

module.exports = {
  STAGE,
  LOGGER_TARGET,
  LOGGER_LEVEL,
};
